# Placeholder for text-to-sketch (later connect SD / DALL·E / local model)
def generate_sketch(prompt: str):
    return {
        "prompt": prompt,
        "note": "Sketch generation model integration pending"
    }
