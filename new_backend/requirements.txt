# ── Core ──
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.0
python-multipart>=0.0.9
pydantic>=2.6.0
pydantic-settings>=2.2.0
loguru>=0.7.0

# ── Database + ORM ──
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0           # PostgreSQL async driver (production)
aiosqlite>=0.20.0         # SQLite async driver (local dev fallback)
alembic>=1.13.0
greenlet>=3.0.0

# ── Firebase Auth (token verification only) ──
firebase-admin>=6.4.0

# ── HTTP client (AI gateway proxy) ──
httpx>=0.27.0
